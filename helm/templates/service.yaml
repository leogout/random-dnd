apiVersion: v1
kind: Service
metadata:
  name: {{ include "random-jdr.fullname" . }}
  labels:
    {{- include "random-jdr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "random-jdr.selectorLabels" . | nindent 4 }}
