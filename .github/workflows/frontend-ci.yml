name: Frontend CI

on:
  pull_request:
    branches: [ main ]

jobs:

  test:
    uses: ./.github/reusable/frontend-setup.yml
    with:
      job-name: Testing
      run-command: npm test

  build:
    needs: [ test ]
    uses: ./.github/reusable/frontend-setup.yml
    with:
      job-name: Building Angular project
      run-command: npm run build -- --configuration production
